def subarraySum(nums, k):
    mp = {0: 1}
    total = 0
    count = 0

    for num in nums:
        total += num

        if total - k in mp:
            count += mp[total - k]

        mp[total] = mp.get(total, 0) + 1

    return count


nums = [1, 1, 1]
k = 2

print(subarraySum(nums, k))
